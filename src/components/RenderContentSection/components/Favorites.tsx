import { iUser } from "../../../providers/User/interfaces";

// tipar favorites
export const Favorites = ({ favorites }: any) => {
  console.log("comentarios");

  return <ul>{/* favorites.map() */}</ul>;
};
